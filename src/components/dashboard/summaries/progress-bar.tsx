import React from 'react'

const ProgressBar = ({
  sections,
  currentSection,
}: {
  sections: any[];
  currentSection: number;
}) => {
  return <div>Progress Bar</div>;
};

export default ProgressBar